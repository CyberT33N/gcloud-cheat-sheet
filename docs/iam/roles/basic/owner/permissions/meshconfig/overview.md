# `roles/owner` — `meshconfig` permissions

`roles/owner` grants 1 permissions in the `meshconfig` service area. Every permission links to its canonical page in the [central permission catalog](../../../../../permissions/overview.md).

## Granted permissions

- [`meshconfig.projects.init`](../../../../../permissions/meshconfig/projects/overview.md)

Role: [`roles/owner`](../../overview.md)
