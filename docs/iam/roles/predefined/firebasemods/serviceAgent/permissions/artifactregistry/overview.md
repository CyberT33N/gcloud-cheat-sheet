# `roles/firebasemods.serviceAgent` — `artifactregistry` permissions

`roles/firebasemods.serviceAgent` grants 1 permissions in the `artifactregistry` service area. Every permission links to its canonical page in the [central permission catalog](../../../../../../permissions/overview.md).

## Granted permissions

- [`artifactregistry.packages.delete`](../../../../../../permissions/artifactregistry/packages/overview.md)

Role: [`roles/firebasemods.serviceAgent`](../../overview.md)
