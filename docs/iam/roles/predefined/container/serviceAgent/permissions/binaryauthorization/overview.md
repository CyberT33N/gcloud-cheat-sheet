# `roles/container.serviceAgent` — `binaryauthorization` permissions

`roles/container.serviceAgent` grants 1 permissions in the `binaryauthorization` service area. Every permission links to its canonical page in the [central permission catalog](../../../../../../permissions/overview.md).

## Granted permissions

- [`binaryauthorization.policy.evaluatePolicy`](../../../../../../permissions/binaryauthorization/policy/overview.md)

Role: [`roles/container.serviceAgent`](../../overview.md)
