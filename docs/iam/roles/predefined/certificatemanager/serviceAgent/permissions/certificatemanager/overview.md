# `roles/certificatemanager.serviceAgent` — `certificatemanager` permissions

`roles/certificatemanager.serviceAgent` grants 1 permissions in the `certificatemanager` service area. Every permission links to its canonical page in the [central permission catalog](../../../../../../permissions/overview.md).

## Granted permissions

- [`certificatemanager.locations.get`](../../../../../../permissions/certificatemanager/locations/overview.md)

Role: [`roles/certificatemanager.serviceAgent`](../../overview.md)
