# `roles/dns.reader` — `compute` permissions

`roles/dns.reader` grants 1 permissions in the `compute` service area. Every permission links to its canonical page in the [central permission catalog](../../../../../../permissions/overview.md).

## Granted permissions

- [`compute.networks.get`](../../../../../../permissions/compute/networks/overview.md)

Role: [`roles/dns.reader`](../../overview.md)
