# `roles/clouddeploymentmanager.serviceAgent` — `source` permissions

`roles/clouddeploymentmanager.serviceAgent` grants 1 permissions in the `source` service area. Every permission links to its canonical page in the [central permission catalog](../../../../../../permissions/overview.md).

## Granted permissions

- [`source.repos.create`](../../../../../../permissions/source/repos/overview.md)

Role: [`roles/clouddeploymentmanager.serviceAgent`](../../overview.md)
