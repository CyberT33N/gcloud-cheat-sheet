# `roles/logging.fieldAccessor` — `logging` permissions

`roles/logging.fieldAccessor` grants 1 permissions in the `logging` service area. Every permission links to its canonical page in the [central permission catalog](../../../../../../permissions/overview.md).

## Granted permissions

- [`logging.fields.access`](../../../../../../permissions/logging/fields/overview.md)

Role: [`roles/logging.fieldAccessor`](../../overview.md)
