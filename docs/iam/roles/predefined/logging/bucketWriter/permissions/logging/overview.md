# `roles/logging.bucketWriter` — `logging` permissions

`roles/logging.bucketWriter` grants 1 permissions in the `logging` service area. Every permission links to its canonical page in the [central permission catalog](../../../../../../permissions/overview.md).

## Granted permissions

- [`logging.buckets.write`](../../../../../../permissions/logging/buckets/overview.md)

Role: [`roles/logging.bucketWriter`](../../overview.md)
