# `roles/securesourcemanager.serviceAgent` — `iam` permissions

`roles/securesourcemanager.serviceAgent` grants 1 permissions in the `iam` service area. Every permission links to its canonical page in the [central permission catalog](../../../../../../permissions/overview.md).

## Granted permissions

- [`iam.serviceAccounts.signJwt`](../../../../../../permissions/iam/serviceAccounts/overview.md)

Role: [`roles/securesourcemanager.serviceAgent`](../../overview.md)
