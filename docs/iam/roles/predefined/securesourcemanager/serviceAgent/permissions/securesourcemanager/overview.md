# `roles/securesourcemanager.serviceAgent` — `securesourcemanager` permissions

`roles/securesourcemanager.serviceAgent` grants 1 permissions in the `securesourcemanager` service area. Every permission links to its canonical page in the [central permission catalog](../../../../../../permissions/overview.md).

## Granted permissions

- [`securesourcemanager.instances.access`](../../../../../../permissions/securesourcemanager/instances/overview.md)

Role: [`roles/securesourcemanager.serviceAgent`](../../overview.md)
