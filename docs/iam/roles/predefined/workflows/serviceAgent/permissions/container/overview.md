# `roles/workflows.serviceAgent` — `container` permissions

`roles/workflows.serviceAgent` grants 1 permissions in the `container` service area. Every permission links to its canonical page in the [central permission catalog](../../../../../../permissions/overview.md).

## Granted permissions

- [`container.clusters.connect`](../../../../../../permissions/container/clusters/overview.md)

Role: [`roles/workflows.serviceAgent`](../../overview.md)
