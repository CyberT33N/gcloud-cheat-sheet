# `roles/cloudbuild.loggingServiceAgent` — `logging` permissions

`roles/cloudbuild.loggingServiceAgent` grants 1 permissions in the `logging` service area. Every permission links to its canonical page in the [central permission catalog](../../../../../../permissions/overview.md).

## Granted permissions

- [`logging.buckets.write`](../../../../../../permissions/logging/buckets/overview.md)

Role: [`roles/cloudbuild.loggingServiceAgent`](../../overview.md)
