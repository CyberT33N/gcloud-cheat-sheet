# `roles/monitoring.notificationServiceAgent` — `logging` permissions

`roles/monitoring.notificationServiceAgent` grants 1 permissions in the `logging` service area. Every permission links to its canonical page in the [central permission catalog](../../../../../../permissions/overview.md).

## Granted permissions

- [`logging.links.list`](../../../../../../permissions/logging/links/overview.md)

Role: [`roles/monitoring.notificationServiceAgent`](../../overview.md)
