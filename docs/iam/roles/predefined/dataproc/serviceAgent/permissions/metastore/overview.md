# `roles/dataproc.serviceAgent` — `metastore` permissions

`roles/dataproc.serviceAgent` grants 1 permissions in the `metastore` service area. Every permission links to its canonical page in the [central permission catalog](../../../../../../permissions/overview.md).

## Granted permissions

- [`metastore.services.get`](../../../../../../permissions/metastore/services/overview.md)

Role: [`roles/dataproc.serviceAgent`](../../overview.md)
