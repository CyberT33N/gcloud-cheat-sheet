# `roles/dataproc.worker` — `datalineage` permissions

`roles/dataproc.worker` grants 1 permissions in the `datalineage` service area. Every permission links to its canonical page in the [central permission catalog](../../../../../../permissions/overview.md).

## Granted permissions

- [`datalineage.locations.processOpenLineageMessage`](../../../../../../permissions/datalineage/locations/overview.md)

Role: [`roles/dataproc.worker`](../../overview.md)
