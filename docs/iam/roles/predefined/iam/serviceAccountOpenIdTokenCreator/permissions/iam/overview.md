# `roles/iam.serviceAccountOpenIdTokenCreator` — `iam` permissions

`roles/iam.serviceAccountOpenIdTokenCreator` grants 1 permissions in the `iam` service area. Every permission links to its canonical page in the [central permission catalog](../../../../../../permissions/overview.md).

## Granted permissions

- [`iam.serviceAccounts.getOpenIdToken`](../../../../../../permissions/iam/serviceAccounts/overview.md)

Role: [`roles/iam.serviceAccountOpenIdTokenCreator`](../../overview.md)
