# `roles/firebase.sdkProvisioningServiceAgent` — `firebase` permissions

`roles/firebase.sdkProvisioningServiceAgent` grants 1 permissions in the `firebase` service area. Every permission links to its canonical page in the [central permission catalog](../../../../../../permissions/overview.md).

## Granted permissions

- [`firebase.clients.create`](../../../../../../permissions/firebase/clients/overview.md)

Role: [`roles/firebase.sdkProvisioningServiceAgent`](../../overview.md)
