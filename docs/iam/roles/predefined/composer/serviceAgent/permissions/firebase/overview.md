# `roles/composer.serviceAgent` — `firebase` permissions

`roles/composer.serviceAgent` grants 1 permissions in the `firebase` service area. Every permission links to its canonical page in the [central permission catalog](../../../../../../permissions/overview.md).

## Granted permissions

- [`firebase.projects.get`](../../../../../../permissions/firebase/projects/overview.md)

Role: [`roles/composer.serviceAgent`](../../overview.md)
