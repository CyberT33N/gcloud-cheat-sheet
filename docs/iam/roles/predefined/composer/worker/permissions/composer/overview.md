# `roles/composer.worker` — `composer` permissions

`roles/composer.worker` grants 1 permissions in the `composer` service area. Every permission links to its canonical page in the [central permission catalog](../../../../../../permissions/overview.md).

## Granted permissions

- [`composer.environments.get`](../../../../../../permissions/composer/environments/overview.md)

Role: [`roles/composer.worker`](../../overview.md)
