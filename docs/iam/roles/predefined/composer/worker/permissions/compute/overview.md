# `roles/composer.worker` — `compute` permissions

`roles/composer.worker` grants 1 permissions in the `compute` service area. Every permission links to its canonical page in the [central permission catalog](../../../../../../permissions/overview.md).

## Granted permissions

- [`compute.images.create`](../../../../../../permissions/compute/images/overview.md)

Role: [`roles/composer.worker`](../../overview.md)
