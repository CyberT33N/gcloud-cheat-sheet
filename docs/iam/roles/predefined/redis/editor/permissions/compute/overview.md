# `roles/redis.editor` — `compute` permissions

`roles/redis.editor` grants 1 permissions in the `compute` service area. Every permission links to its canonical page in the [central permission catalog](../../../../../../permissions/overview.md).

## Granted permissions

- [`compute.networks.list`](../../../../../../permissions/compute/networks/overview.md)

Role: [`roles/redis.editor`](../../overview.md)
