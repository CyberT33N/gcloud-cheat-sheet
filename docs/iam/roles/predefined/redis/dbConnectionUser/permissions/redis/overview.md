# `roles/redis.dbConnectionUser` — `redis` permissions

`roles/redis.dbConnectionUser` grants 1 permissions in the `redis` service area. Every permission links to its canonical page in the [central permission catalog](../../../../../../permissions/overview.md).

## Granted permissions

- [`redis.clusters.connect`](../../../../../../permissions/redis/clusters/overview.md)

Role: [`roles/redis.dbConnectionUser`](../../overview.md)
