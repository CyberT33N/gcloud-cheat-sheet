# `roles/securedlandingzone.serviceAgent` — `logging` permissions

`roles/securedlandingzone.serviceAgent` grants 1 permissions in the `logging` service area. Every permission links to its canonical page in the [central permission catalog](../../../../../../permissions/overview.md).

## Granted permissions

- [`logging.logEntries.list`](../../../../../../permissions/logging/logEntries/overview.md)

Role: [`roles/securedlandingzone.serviceAgent`](../../overview.md)
