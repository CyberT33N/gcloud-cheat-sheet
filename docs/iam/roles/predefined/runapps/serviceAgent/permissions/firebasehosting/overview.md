# `roles/runapps.serviceAgent` — `firebasehosting` permissions

`roles/runapps.serviceAgent` grants 1 permissions in the `firebasehosting` service area. Every permission links to its canonical page in the [central permission catalog](../../../../../../permissions/overview.md).

## Granted permissions

- [`firebasehosting.sites.get`](../../../../../../permissions/firebasehosting/sites/overview.md)

Role: [`roles/runapps.serviceAgent`](../../overview.md)
